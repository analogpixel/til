# Ansible display rendered inventory

If you want to see what your ansible inventory will render to:

`ansible-inventory -i inv.yml --graph`


This is a usefull way to see what a dynamic inventory will render to.
