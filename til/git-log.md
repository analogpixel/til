# Git Log

## Search everything for a string
`git log -S "needle" -p ` 

## List by author
`git log --author=""`

## Show code that changed (patch view)
`git log -p`

## Show only changes to one file
`git log -p -- <filename>`

