# Bash test

WIP

## Options in [
- `-n` : len(string) > 0 
- `-z` : len(string) == 0 
- `-a` : `and`
- `-o` : `or`

## [[ vs [
`[[` is a bash builtin, while `[` uses the test command.

`[[` has more features than `[`  

## Options in [[
- `==` pattern match
- `=~` regex match
- `&&` and
- `||` or
